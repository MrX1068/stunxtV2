import React from 'react';
import CommunityListScreen from '../components/screens/CommunityListScreen';

/**
 * ✅ COMMUNITIES ROUTE
 * 
 * Main entry point for the Community List Screen
 * Provides enterprise-grade community management with:
 * - Instant loading from SQLite cache
 * - Real-time updates via WebSocket
 * - Professional UI/UX design
 * - Comprehensive error handling
 */

export default function CommunitiesPage() {
  return <CommunityListScreen />;
}
